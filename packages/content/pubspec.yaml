name: residuum_content
description: Declarative game data for Residuum — floors, creatures, and the
  starting state.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  residuum_core:
    path: ../core

dev_dependencies:
  test: ^1.25.0
