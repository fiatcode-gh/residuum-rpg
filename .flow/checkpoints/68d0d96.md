# Recovery checkpoint — Unit 14, before the Gate C device pass

Written before the first device action, as `flow-execution` section 9
requires. A session or provider failure after this point must be resumable
from this file plus `.flow/ldd/visual-reboot/`.

## Exact tree

- Repository: `/var/home/dhemas/Development/Projects/fiatcode-gh/residuum-rpg`
- Branch: `residuum-visual-reboot-13`
- Head: **`68d0d9691754f56e866eb5baa8d0904c3df2013d`**
- Base of the unit: `5ac1a49`. `main` is `907a4a8` (PR #22 merge).
- Working tree: architect records only. `packages/app` is clean. Nothing is
  pushed; there is no pull request.

## Evidence already accepted, and why it is still fresh

Nothing has changed under `packages/app` since these were taken at `68d0d96`.

- **Package gates at `68d0d96`**, architect-run and reviewer-reproduced:
  `dart format` 0 changed, `flutter analyze` no issues, full `flutter test`
  **1114 passing**.
- **Gate A**, measured at `bbd18b4` and unaffected by the correction round,
  which the closure review proved left `_fitFor` and every chip metric
  untouched: **331.0 / 429.0 / 578.0 dp** at the three densities, against
  caps of 360 / 450 / 600. Widget-test dp, not device dp.
- **Gate B full acceptance review** (`agent://U14Acceptance`) at `8ab8d09`:
  ACCEPT WITH FINDINGS, zero Critical, zero Important, seven Minor.
- **Gate B correction round** `68d0d96`: five findings closed.
- **Gate B scoped closure review** (`agent://U14Closure`) at `68d0d96`:
  CLOSED WITH FINDINGS, **device gate MAY PROCEED**. Its Important finding
  was a wrong suite figure in the correction receipt, since corrected to
  1114 here and in the ledger.

The stability barrier is therefore closed: no production, asset or build
input has changed since the closure review.

## What remains

**Gate C only.** AC11 is not closed; AC7's device half and AC8's greyscale
reading are deferred to it. Everything else closed at Gate B.

Thirteen capsules, A through M, defined in
`.flow/ldd/visual-reboot/units/unit-14/PLAN.md` section "Gate C — the device
pass". Three inherited duties that must not be lost:

- **Capsule J is the ceiling-density crawl** and carries U13.1's hardware
  confirmation. If the `BattleDock` is covered there, **U13.1 reopens**.
- **Capsules F and G are the roster's and the world map's first visual
  baselines** anywhere in this epic.
- **Capsules H, I and J carry the device dp figures**, compared per density
  against Gate A's widget-test numbers. Expected agreement within 2 dp; a
  wider divergence must be explained first, and the likeliest cause is F3 —
  `✳ ✚ ⛒` are absent from Spectral and sit inside chip labels `_fitFor`
  measures.

**Stop-and-escalate, not an executor's call and not a tuning target:** any
tofu, any clipped diagram label, or worst legal combat above 600 dp on
device.

## Device state and its traps

- The pass runs on **`Medium_Phone`**, which **must be user-started**. At the
  time of writing, `adb devices` shows two physical phones
  (`192.168.18.33:38237` Pixel 4a, and an I2219) and **no emulator**. The
  gate is blocked on the user starting it.
- **Back up both save slots and record byte hashes before the first install.**
  The live path is `app_flutter/save.json`, **not**
  `files/app_flutter/save.json`.
- The app's own rotation moves current to previous during play, so the
  previous slot drifts. **Restore from the backups, never from the device**,
  and verify the restored bytes against the pre-install hashes under the same
  comparison scheme.
- This workstation's ImageMagick returns an anomalous `compare -metric AE` on
  some content (about nineteen times the pixel count) while correct on
  identical inputs. Count differing pixels with a difference/threshold/mean
  route.

## Exact next action

1. **Ask the user to start `Medium_Phone`** and confirm it is up. This is the
   only open blocker.
2. Dispatch fresh `flow-evidence-verifier` sessions, **one coherent capsule
   each**, each carrying its own `Evidence capsule:` manifest, with evidence
   written to `.flow/evidence/68d0d96/<capsule-id>/`.
3. **Every capsule cites `68d0d96`**, not `8ab8d09`. The head moved after the
   full acceptance review.
4. Main inspects each receipt and consequential evidence, and owns the
   acceptance decision. Main does not drive the device sequence itself.

Nothing remote is authorized. Push, pull request and merge are each their own
gate and each need the user's explicit word.
