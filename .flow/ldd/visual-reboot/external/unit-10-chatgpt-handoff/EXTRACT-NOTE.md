# Unit 10 handoff extraction note

This archive is rooted at the repository root.

Extract it from the root of `residuum-rpg`; it will create only:

`.flow/ldd/visual-reboot/units/unit-10/`

It intentionally does not overwrite `LEDGER.md` or `RESUME.md`.
Reconcile `LEDGER-DELTA.md` into the canonical ledger after local freshness checks.
