# Extract note

Extract this archive from the repository root.

It creates only:

`.flow/ldd/visual-reboot/units/unit-11/`

It intentionally does not overwrite canonical `LEDGER.md` or `RESUME.md`.
Use `LEDGER-DELTA.md` only after local freshness checks.

The Unit 11 contract is user-approved. `PLAN.md` is **not** implementation
authorization and still needs separate user approval.
