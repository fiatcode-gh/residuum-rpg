# 01 — Active ASCII scene renderer

Start: approved U16 `CONTRACT.md` and `PLAN.md`; suitable feature checkout from base `374ee775e6fd1c29519dab8fe9d597dd38650593` with shared LDD intake edits preserved. No prior U16 production task. References: `../CONTRACT.md` and four images named in `../PLAN.md`. Revalidate touched source if revision differs. Own only `packages/app/lib/game/{dungeon_scene.dart,dungeon_palette.dart,dungeon_material.dart,dungeon_render_style.dart,dungeon_scene_material.dart,glyph_marks.dart}` and affected game/world-screen tests below; no app asset deletion, boot/art warm-up or UI chrome changes. Task 02 independently removes obsolete boot decoding afterward.

## Locked decisions

- `DungeonSceneSnapshot.fromViewState` retains immutable `glyphPlan` cells, dimensions, focus/pan; delete `materialPlan` field/creation. `_DungeonScene._synchronizeComponents` consumes every cell in its original ordered layers, removing `glyphCellsAboveMaterial`, `_material`, `_synchronizeMaterial` and dungeon-art import. Keep `GridGeometry.camera`, `_ClippedMaxViewport`, 36dp `_GlyphComponent`, tap/drag/long-press and projection identity reuse. All known terrain including `# . < >` is text, under actor/node/item glyphs; unknown absent, remembered alpha 0.4; no new terrain background paint in this task. Region `terrainInk` comes from the existing palette. Move `dungeonVoid` to `dungeon_scene.dart` because its current definition dies with `dungeon_scene_material.dart`.
- Renderer-only removal: delete unused active `dungeon_material.dart`, `dungeon_render_style.dart`, `dungeon_scene_material.dart` and material-only tests `test/game/{dungeon_material_test.dart,dungeon_material_paint_test.dart,dungeon_authored_material_test.dart,dungeon_render_style_test.dart}`, `test/art/material_sampling_test.dart` once all imports are migrated. Reduce `DungeonPalette` to its three glyph colours, four presets and `paletteForDungeon`/`paletteForRoad`, **temporarily retaining `RegionMaterial`** (the still-used historical art catalogue and `dungeon_art.dart` import it until Task 02 moves it). The existing boot continues its pre-existing but now-unused dungeon decode for one independently provable handoff; do not modify `main.dart`, `art_assets.dart`, `dungeon_art.dart`, `warm_up_test.dart`, `art_catalogue_test.dart` or asset files here. Simplify `glyphMarkTreatment(GlyphCell cell)`: delete `semanticTerrain`, give every terrain layer scale 1.0, keep actor hierarchy/halo and both outlines. Remove material/stair-only assertions and numeric implementation pins in existing `test/game/glyph_marks_test.dart`; keep observable actor/target shape tests.
- Tests: `test/game/dungeon_scene_test.dart` remove material-component/plan assertions, replace with scene world tests that observe all known terrain text components and their layer/opacity and verify actor above terrain, stable reuse on pan/focus/drawer-only updates, stairs, hit positions and unchanged selected/marked cues. `test/game/glyph_plan_test.dart`, `glyph_marks_test.dart` and root `test/grid_geometry_test.dart` retain secrecy/geometry/actor-shape proof. `test/widget/world_screen_test.dart` migrate `_navigationMaterialBytes`/`_renderCurrentMaterial` and six callers to test actual *live* scene glyph ink/characters for crypt/sea-cave/keep/road entrance and resume; do not just inspect `DungeonSceneHost.palette` or compare an expected plan to itself. Keep each test's navigation/save assertions. Task 02 separately owns the warm-up/catalogue test migration.

## Red → Green and proof

1. First add behavioral tests before production change. `dungeon_scene_test.dart` expects a visible `#` and `.` plus stairs as terrain TextComponents in the Flame world, a remembered known glyph at 0.4, no unknown component, and actor/target above terrain. It **fails Red** today because `glyphCellsAboveMaterial` drops `#`/`.`. A world-entry palette test observes live terrain text ink and fails if the wrong regional palette is used; keep state/save assertions. Record Red assertion and exit.
2. Apply the renderer cutover, delete material-only renderer assertions/tests, adapt navigation proofs. Keep the live scene's projection list identity on pan/drawer changes; do not rebuild hundreds of glyphs on each camera drag. Environment/dungeon startup decode remains unchanged here.
3. From `packages/app`: `flutter test test/game/dungeon_scene_test.dart test/game/glyph_plan_test.dart test/game/glyph_marks_test.dart test/grid_geometry_test.dart test/widget/world_screen_test.dart test/widget/dungeon_scene_bleed_test.dart`; `dart format <touched Dart paths>`; `flutter analyze`. Record whole relevant output and exit status. Main owns final full suite/device, not this executor.

## Executor discretion

Private helper placement and exact live TextComponent finder; choose only within this ownership. Preserve in-code role fonts (`textFace` from tokens), historical palette enum until Task 02 and U13 actor presentation. No art or package-wide asset cleanup.

## Escalate when

Material projection is still consumed outside the named app/test imports; Flame layering prevents glyph visibility without a topology/geometry change; regional live-ink proof cannot be made observable; any protected FOV, input or palette behavior would change.

## Handoff receipt

Report Red assertion/exit, Green focused test/formatter/analyzer exits, actual deleted/retained paths, rendered known/remembered/unknown and road-region proof, any unanswered contradiction. Repository state is a buildable ASCII map with unchanged (now unnecessary) legacy dungeon-image boot decode, ready for Task 02; no device run or publication.
