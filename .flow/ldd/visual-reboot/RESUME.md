# Resume Visual Reboot

- Epic just bootstrapped (2026-09-13) from an approved external planning
  handoff: `external/residuum-visual-reboot-handoff.md` — approved design
  direction, not an execution plan; no implementation authorization yet.
- In flight: nothing. **Next: Unit 0** — read-only design baseline and
  recon: verify the handoff's seam inventory (section 12.4) against current
  code, build the behavior-preservation/change matrix, reconcile conflicts
  explicitly, then propose the Unit 1 (Flame scene foundation) contract for
  review. No production code until that contract is reviewed.
- Locked inherited contracts: section 18 baseline summary in `LEDGER.md`
  (Flame never authoritative game state; graphical glyphs; map-first melee;
  four-region rule; accessibility by shape/word never hue alone) plus
  everything in `../m3/LEDGER.md` (save v3, band lines as controls, house
  method D113, CI/merge flow).
- Traps that can burn the next session: the approved mock PNG is not in the
  repo (user must supply); any `docs/…` path in inherited text is stale
  (docs merged into `.flow/ldd/`); suites run per package directory (no
  root pubspec).
- Open user decisions: mock file placement; art bible lock (before Unit 2's
  material language ideally); whether m3-quests (M3Q) still runs and where.
- Full decision history: this ledger is young — the product's deep history
  is `../legacy/LEDGER.md` (D1–D126) and `../m3/LEDGER.md`.