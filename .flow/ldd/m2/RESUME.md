# Resume M2 — Engine / Loot / Town / QoL

- Closed epic. All four units merged and accepted; hashes in `LEDGER.md`.
- Continuation lives in `../m3/` and `../visual-reboot/` — start from the
  active epic.
- Full M2-era decision history: `../legacy/LEDGER.md` (D-numbers).
- Traps worth carrying: suites run per package directory (no root pubspec);
  same-seed determinism is a tested contract.