# Handoff record — M3B `m3-balance` (CLOSED — merged `936ca5b` via PR #10, D54; folded into the ledger's Handoff history)

- Story: M3B — the rebalance (D49 verdicts, D50 forks).
- Branch: `m3-balance`. Worktree:
  `/var/home/dhemas/Development/Projects/fiatcode/residuum-rpg/.worktrees/m3-balance`
  @ base `b9d7c21` (created by the architect 2026-08-24).
- Spec: `docs/epic/m3-balance-spec-M3B.md`. Prompt:
  `docs/epic/m3-balance-build-prompt.md`. Recon:
  `docs/epic/m3-balance-recon.md`.
- Build session: `residuum-m3-balance [516b70]` (bg), launched by the
  user 2026-08-24; kickoff pointer delivered by the architect (msg
  d6012734…). No ruling vetoed at launch.
- Architect session: the 2026-08-22 background architect (this session).
- Rulings the user may veto before launch (spec rulings 1–11, notably):
  discovery = drop the adjacency union (simpler than D50's "reveal
  routes" wording, same goal); completion ends the run alive from the
  bottom behind a confirm; camp expiry K=3 via hero-level campDay;
  depth-scoped designed-difficulty pin; prices/inn UNCHANGED (scarcity
  heals the currency); bands re-target 0.45–0.80 with crypt 45–70%.
- Known-in-advance: EVERY survivability figure re-pins; the crypt's
  layout goldens must stay byte-identical.

## Mid-flight exchanges

- 2026-08-24, pre-code. Worker confirmed the baseline verbatim (1296 +
  all five survivability lines), raised ONE BLOCKER (spec ruling 8
  unbuildable inside the section-9 fence — the clamp asymmetry is in
  step.dart, not town.dart; the displacement gate is shared wear.dart),
  pre-declared SIX shape deviations, and corrected FIVE spec/recon
  claims. Architect answered within the hour: fence EXTENDED to
  wear.dart + step.dart confined to two hunks (promoted to ledger as
  D51 before the worker continued); all six deviations approved (rider
  on 3: the old roads' encounter-stream identity gets its own pin);
  all five corrections accepted (row-10 no-op was an architect
  arithmetic error; the smoking gun is 10/14 not 13/14; death-after-
  resume keeps the merchant memory — ruled and pinned; the two
  discovery scans answer different questions; the crypt's
  pierce-inertness/cancel-stack map adopted with named counter-levers).
  Worker builds rulings 1–7, 9–11 while 8's fixes land before the
  tuning converges.
- 2026-08-24, mid-build. Rulings 1, 8, 4 landed (534/403/382 green at
  each exit). Ruling 8: step.dart ONE line, wear.dart = wearRefusal
  branch + a pure `_displacedBy` extraction (one rule, one home);
  NEITHER fix moved a survivability figure (the bot never swaps its
  ceiling down nor hits the cap with two to displace) — the tuning
  trail starts from an unmoved baseline. Worker SELF-CORRECTED its
  opening claim: there are THREE discovery scans, and the spec's
  duplicate pair (rumorOnOffer / _firstUntold) was real — its earlier
  "not duplicates" pointed at the wrong core scan (_untold is the
  traveler's, a different question). All three pinned red-first.
  Two pre-declarations approved: (1) the completion sentence splits
  across three surfaces — control "Leave — done", full-width dim
  STATUS line, dialog verbatim (the five-control bottom row would
  ellipsise the spec's wording — the M2Q/M3D device-catch class,
  avoided pre-device this time); (2) the bottom beat is a MOMENT
  (Descended + depth == deepest, not re-fired on resume) paired with
  the dim line as the always-on STATUS — architect adopted the
  moment/status pairing as the documented design.
- 2026-08-24, the tuning stop (the spec's own stop-and-report fired).
  Worker proved the targets fight each other: the pin's optimum deals
  ≥4 to an armour-five fresh hero (crypt 20–30% vs a 45% floor) and
  the cave ignored every damage-side lever (32–35/40 throughout).
  Six-step measured trail mirrored in TUNING-TRAIL.md. Architect ruled
  (D52): spawn tables open (the D50 grant, un-carried by the spec —
  architect defect); ghoul confines to crypt 1–2 exiting the pin by
  SCOPE (exemptions stay {rat, crab}); ghoul pierce re-tunes down with
  trail; cave counts +1 per depth with the drop-income second-order
  effect measured and cave drop chances the next lever before hp.
  Targets stand; no band widening; no pin re-scope. Trail continues
  from step 6.
