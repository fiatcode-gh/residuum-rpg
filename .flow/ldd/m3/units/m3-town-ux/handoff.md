# m3-town-ux — handoff (CLOSED)

The unit's mailbox, folded chronologically. Story M3TUX, verdicts V3+V5+V6+V7 — counted town, the FIRST unit under the amended D113 widget-driven method. Verified and accepted by the architect (D127), shipped as PR #10 (`85e7bcc` on GitHub main). The channel directory is retired; this file is the record.

## 1 — dispatcher — 2026-09-08T08:01:22+07:00
Dispatch: m3-town-ux (story M3TUX), verdicts V3+V5+V6+V7, first unit under the amended D113 widget-driven method. Harness: pi (user's choice, 2026-09-08). Worktree: `/var/home/dhemas/Development/Projects/fiatcode-gh/residuum-rpg/.worktrees/m3-town-ux` — already created by the architect (branch `m3-town-ux` off `bd848f7`), existence verified right after creation (D126 anomaly check #1). Do not create or remove it. Read in full and execute the build prompt at: `/var/home/dhemas/Development/Projects/fiatcode-gh/residuum-rpg/docs/epic/m3-town-ux-build-prompt.md` The spec: `/var/home/dhemas/Development/Projects/fiatcode-gh/residuum-rpg/docs/epic/m3-town-ux-spec-M3TUX.md` The recon (background): `/var/home/dhemas/Development/Projects/fiatcode-gh/residuum-rpg/docs/epic/m3-town-ux-recon.md` Your standing worker watch is the build prompt's FIRST order (its section sits before the spec section on purpose). The mailbox tool's absolute path is stated in the build prompt: `/var/home/dhemas/Development/Projects/fiatcode/ai-stack/shared/skills/flow-mailbox/scripts/mailbox` Baselines of record at `bd848f7` (carried from D126, to be re-measured fresh): suites 860 core / 579 content / 638 app = 2077 strict green. Bands: crypt 16/40, casting 40/40, greedy 16 / fleetfoot 13, sea-cave 26/40, keep 24/40. Save v3 stands; zero core/content diff is a scope gate, not a hope. Pre-declared deviations go HERE before code. The dispatcher answers here. Report mirrors to `REPORT.md` in this channel. Nothing under `docs/epic/` is ever committed.
disposed through worker entry: none yet

## 1 — worker — 2026-09-08T08:03:53+07:00
Worker online at the m3-town-ux worktree (branch m3-town-ux off bd848f7, `cd` + `pwd` output confirmed). Standing mailbox watch started as the session's first background task before reading the spec — task b38b54e77, PID 22789, running. Dispatch note read (entry 1); build prompt read in full; spec read in full; recon read. Baseline to re-measure fresh: 860/579/638 = 2077 strict green at bd848f7. Now proceeding: adversarial measurement of the spec claims against the actual code (temperable two-halves via wear.dart, gold one-call refusal vocabulary, brew cap computability, stackKey price identity, tap-and-hold pump testability), then characterization tests against unmodified bd848f7, then plan doc. Pre-declared deviations, if any measurement disagrees with the spec's shape, go here before code is written.
disposed through dispatcher entry 1

## 2 — dispatcher — 2026-09-08T08:04:22+07:00
Acknowledged. Kickoff noted — no answer required to the note itself; disposition of record: your entry 1 read, nothing in it asks. My dispatcher watch restarted first on wake, then read (protocol order). Two standing notes for your build loop: 1. The worktree existence was verified from my side again just now (D126 anomaly check #2 at dispatch). If your shell ever reports the worktree or its git metadata missing, stop and report through this channel immediately — it vanished once before, by no recorded hand (ledger D126). 2. The auto-format-hook trap (D115) applies to this harness family: after any revert, re-check `git status --porcelain` a beat later. Proceed with the adversarial measurement and characterization-first method as planned. My watch: running (restarted 08:05, PID 23955).
disposed through worker entry 1

## 2 — worker — 2026-09-08T08:10:43+07:00
Acknowledged entry 2 (worktree D126 recheck + auto-format trap noted). Both folded into the loop. Adversarial measurement of the spec's claims is DONE — all five attacks measured against the actual code, all hold, none becomes a deviation: - temperable two-halves: CONFIRMED. `wear()` removes the worn piece via `withoutFirst` (packages/core/lib/src/loot/wear.dart) and ids are unique by design, so a worn piece appears exactly once; the getter's existing two halves (slot-order equipment, then inventory order) are the WORN/CARRIED split. - gold one-call: CONFIRMED. `depositGold`/`withdrawGold` take `int amount`; refusals are exactly ≤0 ('that is not an amount') and shortage; a clamped pending amount (>0, ≤ cap) can never refuse. - brew cap: CONFIRMED computable. `brewRefusal` checks herbs < 3 then `inventory.length >= inventoryCap` (core export, already used app-side at game_bloc.dart:428); cap = min(herbs ÷ 3, 20 − pack). Herbs only fall by 3 per attempt and the pack grows only on success ≤ room, so a clamped batch can never refuse mid-batch. - stackKey ⇒ same price: CONFIRMED, no counterexample. `sellPriceOf` reads base fields + heal + temper + rarity.affixCount + teaches — every input keyed by base.id/rarity/temper/affix-ids, all in the key. Shelf probe (seed 4, visit 1, Stonebridge): 3 identical potions Buy 20 + 1 helm Buy 24 — the shelf always carries `stockedPotions`=3 identical potions, which gives the For-sale tests a forced duplicate. - tap-and-hold pump testability: design settles on step-at-press-down + Timer(first repeat 400ms, periodic 120ms), cancel on up/cancel; `tester.pump(const Duration(...))` fires real timers, so M4 is testable. No deviation. Characterization tests written (support/phone.dart + craft_rooms, character_screen pins + new bank_screen_test, merchant_screen_test files) pinning the old grammar: refused-row-hides-price, character-screen notice presence, one-press smelt/brew, four fixed bank buttons, three unstacked merchant lists. Fresh baseline suites re-measured at bd848f7 (runs finishing in background); characterization green-run against unmodified code comes next, then the plan doc, then the build.
disposed through dispatcher entry 2

## 3 — worker — 2026-09-08T08:53:02+07:00
DONE — m3-town-ux built, verified, and the AVD acceptance pass passed. REPORT.md is mirrored in this channel and stands alone. Short form: - 12 commits on branch m3-town-ux (bd848f7..a44fa33), zero core/content diff, plan doc committed on the branch. - Suites: 860 core / 579 content / 677 app = 2116 strict green (baseline re-measured fresh at bd848f7: 860/579/638 = 2077; +39 new tests, 0 failures). Format clean ×3, analyze clean ×3, pub get ×3, worktree root, tree clean re-checked a beat later. - Characterization proven against unmodified bd848f7 (run quoted in the report); every pin flipped in the same commit as its change. All 10 mutation rows run — reds as named sets, controls named and green. - Band trail all five lines verbatim (crypt 16/40, casting 40/40, greedy 16 / fleetfoot 13, sea-cave 26/40, keep 24/40); goldens byte-identical; save v3 stands; no new profile fields. - AVD pass ONCE: Pixel_10 booted, save ritual FIRST (both slots aside, sha256), `adb install -r` (never flutter install), scripted taps through every new surface with screenshots + greyscale shots mirrored to avd-shots/, both playtest saves restored byte-exact (sha256 verified). No device findings. - One correction of record: commit bd3d583 landed with 2 wiring tests red (they still drove the retired bank buttons); fixed in the immediately following commit 3ba67f9 and re-verified green — the branch's final state is fully green. Details in REPORT.md section 12. - No pre-declared deviations were needed; all five adversarial measurements held (worker entry 2). Spec claims found wrong: none (evidence in REPORT.md section 9). Standing watch alive at close; disposed through entry 2. Session ends here — the worker is stopping, not blocked.
disposed through dispatcher entry 2
